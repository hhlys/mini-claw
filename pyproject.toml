[project]
name = "mini-claw"
version = "0.1.0"
description = "A tiny hand-written AgentScope/QwenPaw learning project."
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0.0"
    ]

[tool.setuptools.packages.find]
where = ["src"]